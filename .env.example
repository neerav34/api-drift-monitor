# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Encryption (hosted-mode auth headers) — generate with: openssl rand -hex 32
AUTH_HEADER_ENCRYPTION_KEY=

# Self-hosted agent ingestion — generate per-API in the dashboard, this is just a fallback/shared secret
INGEST_SHARED_SECRET=

# Alerts
SLACK_TEST_WEBHOOK_URL=

# Deploy correlation
GITHUB_APP_TOKEN=

# LLM diff summaries (pick one free-tier provider)
GROQ_API_KEY=
GEMINI_API_KEY=
